<%= simple_form_for [@patient, @consultation] do |f| %>
  <%= f.input :height %>
  <%= f.input :weight%>
  <%= f.input :report %>

  <%= f.submit "Ajouter une consultation", class: "btn btn-primary" %>
<% end %>
